# Core deep learning — CPU build by default; GPU optional
torch>=2.2.0
torchvision>=0.17.0

# Numerical & data
numpy>=1.26.0
scipy>=1.12.0

# Tokenizer
sentencepiece>=0.2.0
regex>=2024.1.0

# Configuration
pyyaml>=6.0.1
omegaconf>=2.3.0

# Serialization / model IO
safetensors>=0.4.2

# Progress & logging
tqdm>=4.66.0
rich>=13.7.0

# Testing
pytest>=8.0.0
pytest-cov>=4.1.0

# Type checking & linting (dev only)
mypy>=1.9.0
ruff>=0.3.0

# Optional: INT8 quantization on CPU (no CUDA required)
# Uncomment when ready:
# intel-extension-for-pytorch>=2.2.0   # Intel CPU optimizations
